# modulefiles

```bash
# lmod
sudo mkdir -p /opt/modulefiles/Linux
sudo cp -r modulefiles/* /opt/modulefiles/Linux
```

```bash
# environment modules
sudo cp -r modulefiles/* /usr/local/Modules/modulefiles/
```
